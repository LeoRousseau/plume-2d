/** Two times PI (full circle in radians). */
export const TWO_PI = Math.PI * 2

/** Epsilon for floating-point comparisons (parallelism, singularity, tangency). */
export const EPSILON = 1e-10

/** Epsilon for matrix singularity detection. */
export const MATRIX_EPSILON = 1e-12

/** Default number of subdivisions when linearizing Bezier curves. */
export const CURVE_SUBDIVISIONS = 16

/** Default hit-test tolerance in pixels. */
export const DEFAULT_HIT_TOLERANCE = 2

/** Approximate character-width ratio relative to fontSize for text bounding box estimation. */
export const TEXT_CHAR_WIDTH_RATIO = 0.6

/** Approximate baseline offset ratio for "alphabetic" text baseline. */
export const TEXT_ALPHABETIC_RATIO = 0.8
